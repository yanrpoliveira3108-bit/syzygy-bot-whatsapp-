diff --git a/features/flood/config.js b/features/flood/config.js
index 4fdca41..93558ae 100644
--- a/features/flood/config.js
+++ b/features/flood/config.js
@@ -74,7 +74,9 @@ export const FLOOD_PRESETS = {
             surface: 1,
             id: "https://en.wikipedia.org/wiki/QR_code"
         },
-        viewOnce: true,
+        // [FIX] viewOnce: true fazia o fork embrulhar o card em viewOnceMessage
+        // → "mensagem indisponível" / "atualize o WhatsApp" no destinatário.
+        viewOnce: false,
         targetMode: "selected",
         maxMessages: 3,
         interval: 3000,
diff --git a/features/flood/engine.js b/features/flood/engine.js
index e127508..0f8de7c 100644
--- a/features/flood/engine.js
+++ b/features/flood/engine.js
@@ -316,10 +316,28 @@ async function defaultSend(jid, content) {
         return sock.sendMessage(jid, payload)
     }
     if (content && content.shop) {
-        const payload = { ...content }
+        // [FIX] era { ...content }: espalhava viewOnce, hasMediaAttachment:false,
+        // caption:"" e chaves undefined no atalho do fork. Aqui entra SOMENTE o
+        // que o ramo shop lê, e viewOnce só quando é true de verdade.
+        const payload = {}
+        for (const k of ["text", "caption", "title", "subtitle", "footer", "mimetype",
+                         "image", "video", "document", "location", "product", "jpegThumbnail",
+                         "businessOwnerJid", "mentions", "linkPreview"]) {
+            const v = content[k]
+            if (v === undefined || v === null) continue
+            if (typeof v === "string" && !v.trim()) continue
+            if (Array.isArray(v) && !v.length) continue
+            payload[k] = v
+        }
+        payload.shop = { surface: content.shop.surface, id: content.shop.id }
+        if (content.hasMediaAttachment === true) payload.hasMediaAttachment = true
+        if (content.viewOnce === true) payload.viewOnce = true
         if (payload.product && !payload.businessOwnerJid && sock.user?.id) {
             payload.businessOwnerJid = sock.user.id
         }
+        if (payload.shop.surface === 4 || ![1, 2, 3].includes(payload.shop.surface)) {
+            throw Object.assign(new Error("SURFACE_INVALID"), { code: "SURFACE_INVALID" })
+        }
         return sock.sendMessage(jid, payload)
     }
     const { safeSendMessage } = await import("../../services/groupService.js")
diff --git a/features/flood/index.js b/features/flood/index.js
index c91b7eb..34bfc71 100644
--- a/features/flood/index.js
+++ b/features/flood/index.js
@@ -200,7 +200,7 @@ function shoppingContentPrompt(presetId) {
     const title = def.title || "SYZYGY SHOP"
     const surface = def.shop?.surface ?? 1
     const shopId = def.shop?.id || "https://en.wikipedia.org/wiki/QR_code"
-    return `🛍️ CONTEÚDO DA LOJA\nAtual: ${title} · surface ${surface}\n\nCole o texto da loja (uma mensagem)\nOu: texto|title|surface|id\nEx: Produto de teste|SYZYGY SHOP|1|https://en.wikipedia.org/wiki/QR_code\nSurface: 1 · 2 · 3 · 4\n\n0 = manter atual`
+    return `🛍️ CONTEÚDO DA LOJA\nAtual: ${title} · surface ${surface}\n\nCole o texto da loja (uma mensagem)\nOu: texto|title|surface|id\nEx: Produto de teste|SYZYGY SHOP|1|https://en.wikipedia.org/wiki/QR_code\nSurface: 1 (FB) · 2 (IG) · 3 (WA)\n_(o 4 do README do fork não existe no proto → vira 3)_\nviewOnce: OFF (card dentro de visualização única fica indisponível)\n\n0 = manter atual`
 }
 
 function qtdPrompt() {
diff --git a/features/flood/shopping.js b/features/flood/shopping.js
index 1f6e4c0..bcbd47f 100644
--- a/features/flood/shopping.js
+++ b/features/flood/shopping.js
@@ -5,13 +5,35 @@
 //   → proto interactiveMessage.shopStorefrontMessage { id, surface }
 //
 // Formatos no generateWAMessageContent: text, image, video, document, location, product.
-// Surface no README: 1 | 2 | 3 | 4. Proto enum: 0 UNKNOWN, 1 FB, 2 IG, 3 WA.
+//
+// [FIX 2026-09-16] Três coisas que faziam o destinatário ver "mensagem
+// indisponível" + "sua versão do WhatsApp não é compatível" (medido no pacote
+// real com generateWAMessageContent + proto.Message.encode, não por README):
+//
+//  1. viewOnce. `viewOnce = src.viewOnce !== false` = SEMPRE true, e o fork faz
+//     `else if ('viewOnce' in message && !!message.viewOnce) m = { viewOnceMessage: { message: m } }`
+//     → interactiveMessage DENTRO de viewOnceMessage. Agora: a chave só existe
+//     quando viewOnce === true; senão é OMITIDA (não mandamos false).
+//  2. surface. O enum do proto é 0 UNKNOWN · 1 FB · 2 IG · 3 WA. O README lista
+//     4, e o ShopMessage.fromObject do protobufjs aceita QUALQUER número no
+//     default: → 4 ia cru no wire. Agora: 1-3 passam, 4 → 3 (WA) com aviso
+//     mapeado, resto SURFACE_INVALID.
+//  3. Chaves vazias. caption:"" e hasMediaAttachment:false iam no objeto; no
+//     ramo text do fork isso gera header/footer com campo vazio. Agora: só vai
+//     o que tem valor real.
+//
+// messageVersion: o ramo shop PURO não expõe o campo; não montamos proto cru,
+// então não o inventamos. (O ramo nativeFlow+shop é o único que põe =1.)
 // Não inventar API. Se shop não estiver no content, o adaptador recusa.
 
 import { parseCurrency } from "./payment.js"
 
 export const SHOP_FORMATS = ["text", "image", "video", "document", "location", "product"]
-export const SHOP_SURFACES = new Set([1, 2, 3, 4])
+export const SHOP_SURFACES = new Set([1, 2, 3])
+// 4 existe só no README do fork. Mapear para 3 (WA) é a decisão do projeto:
+// enviar 4 = cliente "versão incompatível" sem atualização existir.
+export const SURFACE_README_ALIAS = { 4: 3 }
+const SURFACE_NOMES = { fb: 1, ig: 2, wa: 3 }
 
 export function getShoppingApiInfo() {
     return {
@@ -27,11 +49,15 @@ export function getShoppingApiInfo() {
 
 export function parseSurface(raw) {
     if (raw == null || String(raw).trim() === "") return { ok: false, error: "SURFACE_MISSING" }
-    const s = String(raw).trim()
-    if (!/^[1-4]$/.test(s)) return { ok: false, error: "SURFACE_INVALID" }
-    const n = Number(s)
-    if (!SHOP_SURFACES.has(n)) return { ok: false, error: "SURFACE_INVALID" }
-    return { ok: true, value: n }
+    const s = String(raw).trim().toLowerCase()
+    let n
+    if (Object.prototype.hasOwnProperty.call(SURFACE_NOMES, s)) n = SURFACE_NOMES[s]
+    else if (/^\d+$/.test(s)) n = Number(s)
+    else return { ok: false, error: "SURFACE_INVALID" }
+    if (!Number.isInteger(n)) return { ok: false, error: "SURFACE_INVALID" }
+    if (SHOP_SURFACES.has(n)) return { ok: true, value: n }
+    if (SURFACE_README_ALIAS[n]) return { ok: true, value: SURFACE_README_ALIAS[n], mapped: n }
+    return { ok: false, error: "SURFACE_INVALID" }
 }
 
 export function parseShopId(raw) {
@@ -203,11 +229,12 @@ export function createShoppingPayload(src = {}) {
     const title = optionalStr(src.title)
     const subtitle = optionalStr(src.subtitle)
     const footer = optionalStr(src.footer)
-    const viewOnce = src.viewOnce !== false
+    // viewOnce SÓ quando pedido explicitamente; sem a chave, sem wrap viewOnceMessage.
+    const viewOnce = src.viewOnce === true
     const content = {
-        shop: { surface: surface.value, id: shopId.value },
-        viewOnce
+        shop: { surface: surface.value, id: shopId.value }
     }
+    if (viewOnce) content.viewOnce = true
     if (title) content.title = title
     if (subtitle) content.subtitle = subtitle
     if (footer) content.footer = footer
@@ -220,14 +247,16 @@ export function createShoppingPayload(src = {}) {
         const img = mediaUpload(src.image)
         if (!img.ok) return { ...img, usage: usageTexto() }
         content.image = img.value
-        content.caption = optionalStr(src.caption) || optionalStr(src.text) || title || ""
-        content.hasMediaAttachment = src.hasMediaAttachment === true
+        const cap = optionalStr(src.caption) || optionalStr(src.text) || title
+        if (cap) content.caption = cap
+        if (src.hasMediaAttachment === true) content.hasMediaAttachment = true
     } else if (format === "video") {
         const vid = mediaUpload(src.video)
         if (!vid.ok) return { ...vid, usage: usageTexto() }
         content.video = vid.value
-        content.caption = optionalStr(src.caption) || optionalStr(src.text) || title || ""
-        content.hasMediaAttachment = src.hasMediaAttachment === true
+        const cap = optionalStr(src.caption) || optionalStr(src.text) || title
+        if (cap) content.caption = cap
+        if (src.hasMediaAttachment === true) content.hasMediaAttachment = true
     } else if (format === "document") {
         const doc = mediaUpload(src.document)
         if (!doc.ok) return { ...doc, usage: usageTexto() }
@@ -235,15 +264,17 @@ export function createShoppingPayload(src = {}) {
         if (!mimetype) return { ok: false, error: "MIMETYPE_MISSING", usage: usageTexto() }
         content.document = doc.value
         content.mimetype = mimetype
-        content.caption = optionalStr(src.caption) || optionalStr(src.text) || title || ""
+        const capDoc = optionalStr(src.caption) || optionalStr(src.text) || title
+        if (capDoc) content.caption = capDoc
         if (src.jpegThumbnail != null) content.jpegThumbnail = src.jpegThumbnail
-        content.hasMediaAttachment = src.hasMediaAttachment === true
+        if (src.hasMediaAttachment === true) content.hasMediaAttachment = true
     } else if (format === "location") {
         const loc = parseLocation(src.location)
         if (!loc.ok) return { ...loc, usage: usageTexto() }
         content.location = loc.value
-        content.caption = optionalStr(src.caption) || optionalStr(src.text) || title || ""
-        content.hasMediaAttachment = src.hasMediaAttachment === true
+        const cap = optionalStr(src.caption) || optionalStr(src.text) || title
+        if (cap) content.caption = cap
+        if (src.hasMediaAttachment === true) content.hasMediaAttachment = true
     } else if (format === "product") {
         const product = parseProduct(src.product)
         if (!product.ok) return { ...product, usage: usageTexto() }
@@ -253,8 +284,9 @@ export function createShoppingPayload(src = {}) {
             if (!owner.ok) return { ...owner, usage: usageTexto() }
             content.businessOwnerJid = owner.value
         }
-        content.caption = optionalStr(src.caption) || optionalStr(src.text) || title || ""
-        content.hasMediaAttachment = src.hasMediaAttachment === true
+        const cap = optionalStr(src.caption) || optionalStr(src.text) || title
+        if (cap) content.caption = cap
+        if (src.hasMediaAttachment === true) content.hasMediaAttachment = true
     }
 
     return {
@@ -265,6 +297,7 @@ export function createShoppingPayload(src = {}) {
         subtitle: content.subtitle,
         footer: content.footer,
         surface: surface.value,
+        surfaceMapped: surface.mapped || null,
         shopId: shopId.value,
         viewOnce,
         content
@@ -279,8 +312,8 @@ export function formatShoppingError(code) {
     const map = {
         USAGE: usageTexto(),
         TEXT_MISSING: `Texto/body obrigatório.\n${usageTexto()}`,
-        SURFACE_MISSING: `surface obrigatório (1, 2, 3 ou 4).\n${usageTexto()}`,
-        SURFACE_INVALID: `surface inválido. Use 1, 2, 3 ou 4.\n${usageTexto()}`,
+        SURFACE_MISSING: `surface obrigatório: 1 (FB), 2 (IG) ou 3 (WA).\nO 4 que aparece no README do fork não existe no proto deste pacote (ShopMessage.Surface = 0..3).\n${usageTexto()}`,
+        SURFACE_INVALID: `surface inválido. Use 1 (FB), 2 (IG) ou 3 (WA) — o 4 do README é aceito e vira 3; 0 e 5+ não existem no proto.\n${usageTexto()}`,
         SHOP_ID_MISSING: `shop.id obrigatório.\n${usageTexto()}`,
         SHOP_ID_INVALID: `shop.id inválido. Use URL http(s) ou id da loja.\n${usageTexto()}`,
         URL_MISSING: "URL obrigatória.",
diff --git a/features/flood/tests.js b/features/flood/tests.js
index d3182b4..5e553d1 100644
--- a/features/flood/tests.js
+++ b/features/flood/tests.js
@@ -72,20 +72,31 @@ export async function runFloodPresetTests() {
         assert(parseShoppingArgs("Produto|Title|9|https://example.com").error === "SURFACE_INVALID", "surface 9 inválido")
         assert(parseShoppingArgs("Produto|Title|0|https://example.com").error === "SURFACE_INVALID", "surface 0 inválido")
         assert(parseShoppingArgs("Produto|Title|abc|https://example.com").error === "SURFACE_INVALID", "surface abc inválido")
-        assert(parseSurface(1).ok && parseSurface("2").ok && parseSurface(3).ok && parseSurface("4").ok, "surface 1-4 válidos")
+        assert(parseSurface(1).ok && parseSurface("2").ok && parseSurface(3).ok, "surface 1-3 válidos (enum do proto)")
+        assert(parseSurface("4").ok && parseSurface("4").value === 3 && parseSurface("4").mapped === 4, "surface 4 do README é ACEITO e MAPEADO para 3 (WA) — nunca vai cru")
+        assert(parseSurface(0).error === "SURFACE_INVALID" && parseSurface(5).error === "SURFACE_INVALID", "surface 0/5+ fora do enum → SURFACE_INVALID")
+        assert(parseSurface("wa").ok && parseSurface("wa").value === 3, "surface aceita o nome do enum (wa → 3)")
         assert(parseShopId("").error === "SHOP_ID_MISSING", "shop.id vazio")
         assert(parseShopId("https://").error === "SHOP_ID_INVALID", "shop.id URL inválida")
         const shBody = parseShoppingArgs("É O TERROR 🙊|by zuck|4|https://en.wikipedia.org/wiki/QR_code")
-        assert(shBody.ok && shBody.surface === 4 && shBody.title === "by zuck" && shBody.text.includes("É O TERROR"), "shopping texto com | no meio + title|surface|id")
+        assert(shBody.ok && shBody.surface === 3 && shBody.surfaceMapped === 4 && shBody.title === "by zuck" && shBody.text.includes("É O TERROR"), "overlay |by zuck|4|url → send com surface 3 (4 NUNCA vai no wire)")
+        assert(shBody.content.shop.surface === 3 && shBody.content.shop.surface !== 4, "payload do overlay não contém surface 4")
+        assert(!("viewOnce" in shBody.content), "overlay da loja não introduz viewOnce")
         const sh1 = parseShoppingArgs("Produto de teste|SYZYGY SHOP|1|https://en.wikipedia.org/wiki/QR_code")
         assert(sh1.ok && sh1.surface === 1 && sh1.content.shop.id.startsWith("https://"), "shopping texto|title|surface|id")
         assert(sh1.content.text === "Produto de teste" && sh1.content.title === "SYZYGY SHOP", "shopping text+title")
-        assert(sh1.content.shop && sh1.content.viewOnce === true, "shopping shop + viewOnce")
+        assert(sh1.content.shop && !("viewOnce" in sh1.content), "shopping NÃO manda viewOnce por padrão (sem wrap viewOnceMessage)")
+        assert(sh1.viewOnce === false, "meta do payload reporta viewOnce false")
+        assert(!("hasMediaAttachment" in sh1.content), "formato text não leva hasMediaAttachment")
+        assert(createShoppingPayload({ text: "t", shop: { surface: 1, id: "https://x" }, viewOnce: true }).content.viewOnce === true, "viewOnce:true EXPLÍCITO continua respeitado")
+        assert(!("viewOnce" in createShoppingPayload({ text: "t", shop: { surface: 1, id: "https://x" }, viewOnce: false }).content), "viewOnce:false → chave omitida (não enviamos false)")
+        assert(FLOOD_PRESETS["shopping-test"].viewOnce === false, "preset shopping-test não nasce com viewOnce: true")
         assert(!("payment" in sh1.content) && !("image" in sh1.content), "shopping text sem payment/image")
         const shopBuilt = buildShoppingContent(sh1)
         assert(shopBuilt.shop.surface === 1 && shopBuilt.text === "Produto de teste", "buildShoppingContent API shop")
         assert(getShoppingApiInfo().proto === "interactiveMessage.shopStorefrontMessage", "API documentada = shopStorefrontMessage")
-        assert(formatShoppingError("SURFACE_INVALID").includes("1, 2, 3 ou 4"), "erro surface amigável")
+        assert(formatShoppingError("SURFACE_INVALID").includes("1 (FB)"), "erro surface cita os valores do proto")
+        assert(formatShoppingError("SURFACE_INVALID").includes("README"), "erro surface explica o 4 do README")
         const imgShop = createShoppingPayload({
             format: "image",
             image: "https://example.com/image.jpg",
@@ -94,6 +105,8 @@ export async function runFloodPresetTests() {
             shop: { surface: 2, id: "https://example.com" }
         })
         assert(imgShop.ok && imgShop.content.image && imgShop.content.caption === "Look" && !("text" in imgShop.content), "shopping image sem text")
+        const imgNoCap = createShoppingPayload({ format: "image", image: "https://example.com/i.jpg", shop: { surface: 2, id: "https://example.com" }, hasMediaAttachment: false })
+        assert(!("caption" in imgNoCap.content) && !("hasMediaAttachment" in imgNoCap.content) && !("viewOnce" in imgNoCap.content), "mídia sem legenda: nada vazio/undefined espalhado no payload")
         const vidShop = createShoppingPayload({
             format: "video",
             video: "https://example.com/video.mp4",
